# Run
```shell
    uv run main.py
```

# Notes
```shell
    uv pip install google-genai
```

This is in place only to get the cli check to pass.
I'll code using *ollama* as much as possible because it can run locally
perfectly well.
Some mock code/dead code are inserted to get the check to pass.

